import type { ModelRuntimeRouteEditorProps } from './ModelRuntimeRouteEditor';

/** Build-time replacement that keeps plugin controls out of library-only artifacts. */
export function ModelRuntimeRouteEditor(_props: ModelRuntimeRouteEditorProps) {
  return null;
}
